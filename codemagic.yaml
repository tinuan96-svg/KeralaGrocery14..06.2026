workflows:
  ios-release:
    name: Kerala Grocery iOS Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      groups:
        - supabase
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.keralagrocery.app
      vars:
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
        APP_ID: "com.keralagrocery.app"
        TEAM_ID: "PDZ7779Q9J"
      node: 22
      xcode: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'KeralaGrocery.com'
          include: true
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build Next.js project
        script: |
          export NEXT_TELEMETRY_DISABLED=1
          npm run build
          mkdir -p out
          echo "<html><body>Kerala Grocery Remote Wrapper</body></html>" > out/index.html
      - name: Sync Capacitor and Patch Configs
        script: |
          # 1. Sync Capacitor
          npx cap sync ios
          
          # 2. Apply custom patches
          node scripts/patch-info-plist.js
          cp ios-config/Podfile.patch ios/App/Podfile
          
          # 3. Generate Flattened App Icons (No alpha channel)
          node scripts/generate-ios-icons.js
          
          # 4. Install Pods
          cd ios/App
          rm -rf Pods
          rm -rf Podfile.lock
          pod install
      - name: Set build number
        script: |
          cd ios/App
          # Using 50 as a safe starting point to clear all previous failed uploads
          agvtool new-version -all $((50 + $BUILD_NUMBER))
      - name: Build iOS IPA
        script: |
          keychain initialize
          keychain add-certificates
          
          # Use the profile reference name we set in the UI
          xcode-project use-profiles --profile keralagrocery_app_store --archive-method app-store
          
          # Final fix for export error and duplicate frameworks
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME" \
            --archive-flags "DEVELOPMENT_TEAM=$TEAM_ID ENABLE_USER_SCRIPT_SANDBOXING=NO" \
            --archive-xcargs "COMPILER_INDEX_STORE_ENABLE=NO SKIP_INSTALL=NO"
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
